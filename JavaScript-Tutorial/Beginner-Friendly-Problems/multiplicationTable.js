// This program prints the multiplication table for a given number
let number = 5;  // Change the number here

for (let i = 1; i <= 10; i++) {
    console.log(number + " x " + i + " = " + (number * i));
}
